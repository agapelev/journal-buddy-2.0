# 📚 DEVELOPMENT.md — Полный гайд разработки Journal with Gemini

**Версия:** 1.0.0  
**Дата создания:** 8 февраля 2026  
**Автор:** Lev Nikolaevich  
**Лицензия:** Apache 2.0

---

## 📖 Содержание

1. [Описание проекта](#описание-проекта)
2. [Что было сделано](#что-было-сделано)
3. [Архитектура приложения](#архитектура-приложения)
4. [Пошаговая настройка окружения](#пошаговая-настройка-окружения)
5. [Как работает приложение](#как-работает-приложение)
6. [Цели для доработки](#цели-для-доработки)
7. [Процесс разработки и коммиты](#процесс-разработки-и-коммиты)

---

## 📔 Описание проекта

### Что это такое?

**Journal with Gemini** — это веб-приложение для разработчиков, которое позволяет:
- 📝 Вести два типа журналов одновременно
- 🤖 Анализировать записи с помощью Google Gemini 3 Flash AI
- 💾 Сохранять данные в браузере (без сервера)
- ⚡ Использовать в offline режиме

### Для кого?

- 👨‍💻 Разработчики, которые хотят документировать свои решения
- 🧠 Люди, которые ведут рефлексию и философские размышления
- 🎯 Те, кто хочет анализировать свой прогресс с помощью ИИ

### Основная идея

**Цифровое трезвение** — осознанный выбор инструментов. ИИ — это усилитель намеръний, а не замена человеку.

> *"Если в сердце мир — ИИ распространит мир. Если хаос — ИИ его умножит."*

---

## ✅ Что было сделано

### Фаза 1: Базовая структура (До v1.0.0)
- ✅ Создана базовая структура Angular приложения
- ✅ Два компонента журналов (Dev Log + AI Insights)
- ✅ Интеграция с Google Generative AI (Gemini API)
- ✅ Вывод ответов моделей в UI

### Фаза 2: Персистентность данных (v1.0.0)
- ✅ **LocalStorage** для сохранения записей между сеансами
- ✅ **SessionStorage** для безопасного хранения API ключа
- ✅ Автоматическая загрузка данных при старте приложения
- ✅ Использование `effect()` для синхронизации Signals с хранилищем

### Фаза 3: Безопасность и обработка ошибок
- ✅ Удалены хардкодированные API ключи из скриптов
- ✅ Переход на переменные окружения (Environment Variables)
- ✅ Улучшена обработка ошибок с точной диагностикой:
  - 401 ошибка — неверный ключ
  - 403 ошибка — нет прав доступа
  - 429 ошибка — исчерпана квота
- ✅ Кнопка очистки сохраненного ключа в UI

### Фаза 4: Production-готовность
- ✅ Полностью переписан README.md с инструкциями
- ✅ Добавлены кнопки для быстрого деплоя (1-click deploy)
- ✅ Конфиги для всех популярных платформ:
  - `vercel.json` — Vercel
  - `netlify.toml` — Netlify
  - `wrangler.toml` — Cloudflare Pages
- ✅ `.env.example` — пример конфигурации
- ✅ Обновлен `.gitignore` для защиты sensitive файлов
- ✅ Git коммит v1.0.0 с полной историей

### Фаза 5: Документация
- ✅ Создан этот файл DEVELOPMENT.md

---

## 🏗️ Архитектура приложения

### Структура файлов

```
src/
├── app/
│   ├── app.component.ts           # Главный компонент (выбор журнала)
│   ├── journal.component.ts       # Основной журнал + вопросы к Gemini
│   ├── journal-entries.ts         # Сервис с LocalStorage
│   ├── entry.component.ts         # Компонент отдельной записи
│   ├── app.config.ts              # Конфигурация приложения
│   └── index.html                 # HTML шаблон
├── main.ts                        # Entry point Angular
└── styles.css                     # Глобальные стили
```

### Поток данных

```
┌─────────────────────────────────┐
│     AppComponent               │
│  (Выбор журнала + API ключ)    │
└──────────────┬──────────────────┘
               │
        (передает ключ и выбор)
               │
┌──────────────▼──────────────────┐
│   JournalComponent              │
│  (Основной интерфейс)          │
└──────────────┬──────────────────┘
               │
        (использует)
               │
     ┌─────────┴──────────┐
     │                    │
┌────▼────────────────┐  ┌────▼──────────────┐
│ JournalEntries      │  │ GoogleGenerativeAI│
│ (LocalStorage)      │  │ (Gemini API)      │
└─────────────────────┘  └───────────────────┘
```

### Компоненты и их роли

#### 1. **AppComponent** (`app.component.ts`)
**Ответственность:** Главная страница выбора

**Что делает:**
- Показывает экран входа с полем для API ключа
- Позволяет выбрать один из двух журналов
- Сохраняет ключ в SessionStorage
- Имеет кнопку "Очистить ключ"

**Ключевые методы:**
```typescript
ngOnInit()              // Загружает ключ из SessionStorage при старте
onApiKeyChange(value)   // Сохраняет ключ при изменении
clearApiKey()           // Очищает ключ и SessionStorage
select_dev_log()        // Переходит в Dev Log
select_ai_insights()    // Переходит в AI Insights
```

**Хранилище:**
- `api_key` — текущий API ключ (string)
- `selected_journal` — выбранный журнал (signal)
- `API_KEY_STORAGE_KEY` — ключ для SessionStorage

#### 2. **JournalComponent** (`journal.component.ts`)
**Ответственность:** Основной интерфейс журнала

**Что делает:**
- Отображает список записей
- Позволяет добавлять новые записи
- Позволяет задавать вопросы Gemini
- Показывает ответы модели в реальном времени

**Ключевые методы:**
```typescript
addNewEntry()           // Добавляет запись в журнал
ask_question()          // Отправляет вопрос в Gemini
ask(question_to_ask)    // Обрабатывает запрос
callGemini(prompt)      // Вызывает REST API Gemini
```

**Состояние:**
- `newEntryText` — текст новой записи
- `question` — текущий вопрос
- `answer` — ответ от Gemini
- `loading` — флаг загрузки
- `error_message` — текст ошибки

#### 3. **JournalEntries** (`journal-entries.ts`)
**Ответственность:** Управление хранилищем записей

**Что делает:**
- Управляет Signals для обоих журналов
- Автоматически сохраняет в LocalStorage через `effect()`
- Загружает данные из LocalStorage при инициализации
- Предоставляет методы добавления записей

**Ключевые методы:**
```typescript
constructor()           // Инициализирует, загружает из LocalStorage
loadFromStorage()       // Восстанавливает данные из браузера
getEntries(journal)    // Возвращает записи выбранного журнала
addEntryToJournal()    // Добавляет новую запись
```

**Signals:**
- `dev_log_signal` — сигнал Dev Log записей
- `ai_insights_signal` — сигнал AI Insights записей

#### 4. **EntryComponent** (`entry.component.ts`)
**Ответственность:** Визуализация отдельной записи

**Что делает:**
- Отображает одну запись
- Расцвечивает метаданные (между `---`)
- Форматирует текст красиво

**Ключевые методы:**
```typescript
processedLines()        // Обрабатывает текст записи (computed property)
```

---

## 🔧 Пошаговая настройка окружения

### Шаг 1: Установка Node.js и npm

#### Windows/Mac
1. Скачай https://nodejs.org/
2. Выбери **LTS версию** (рекомендуется 18.x или 20.x)
3. Установи, следуя инструкциям
4. Проверь установку в терминале:
```bash
node --version    # Должно вывести v18.x.x или выше
npm --version     # Должно вывести 9.x.x или выше
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install nodejs npm
node --version
npm --version
```

#### Linux (Fish Shell, как у Льва)
```fish
# Установка через brew (если brew установлен)
brew install node

# Или через системный пакетный менеджер
sudo apt install nodejs npm

# Проверка
node --version
npm --version
```

### Шаг 2: Клонирование репозитория

```bash
# Откройте терминал в папке, где хотите работать
# Windows: Откройте PowerShell или CMD
# Mac/Linux: Откройте Terminal

# Клонируйте репозиторий
git clone https://github.com/your-username/Journal-with-Gemini.git

# Перейдите в папку проекта
cd Journal-with-Gemini

# Проверьте, что вы в нужной папке (должен быть файл package.json)
ls    # или dir на Windows
```

### Шаг 3: Установка зависимостей

```bash
# Установи все npm пакеты (это может занять 2-5 минут)
npm install

# Проверь, что всё установилось (должна появиться папка node_modules)
ls node_modules
```

### Шаг 4: Запуск локального сервера

```bash
# Запусти dev сервер
npm start

# Или более явно:
ng serve

# Вывод должен быть похож на:
# ➜  Local:   http://localhost:4200/
# ➜  Network: http://192.168.1.34:4200/

# Откройте в браузере: http://localhost:4200
```

### Шаг 5: Получение API ключа Gemini

1. Откройте https://aistudio.google.com/apikey
2. Нажмите **"Create API key in new project"**
3. Скопируйте ключ (убедитесь, что нет пробелов)
4. Вставьте в приложение в поле "Gemini API key"

### Шаг 6: Тестирование приложения

1. **Выберите журнал** (🛠 Dev Log или 🧠 AI Insights)
2. **Добавьте запись:**
   - Напишите что-то в поле "Записать новое событие"
   - Нажмите "Сохранить в дневник"
3. **Задайте вопрос:**
   - Напишите вопрос в поле "Спросите Gemini"
   - Нажмите "Спросить Близнецов"
   - Должен получить ответ от модели
4. **Перезагрузите страницу (F5):**
   - Записи должны остаться (благодаря LocalStorage)
   - Ключ должен восстановиться (благодаря SessionStorage)

---

## 🎯 Как работает приложение

### Сценарий 1: Первый запуск

```
1. Пользователь открывает http://localhost:4200
   ↓
2. AppComponent загружается (ngOnInit вызывается)
   - Проверяет SessionStorage на наличие сохраненного ключа
   - Если есть → заполняет поле автоматически
   - Если нет → поле пустое
   ↓
3. Пользователь вводит API ключ
   - onApiKeyChange() срабатывает при каждом нажатии
   - Ключ сохраняется в SessionStorage
   ↓
4. Пользователь выбирает журнал (Dev Log или AI Insights)
   - selected_journal.set("dev_log") или "ai_insights"
   - Условие @if меняется, показывается JournalComponent
   ↓
5. JournalComponent загружается
   - journalEntries.getEntries() возвращает список записей
   - @for цикл отрисовывает каждую запись через EntryComponent
```

### Сценарий 2: Добавление записи

```
1. Пользователь пишет текст в textarea
   - newEntryText обновляется (two-way binding)
   ↓
2. Пользователь нажимает "Сохранить в дневник"
   - addNewEntry() вызывается
   ↓
3. addNewEntry() обрабатывает:
   - Проверяет что текст не пуст
   - Создает объект JournalEntry с датой и текстом
   - Вызывает journalEntries.addEntryToJournal()
   ↓
4. journalEntries.addEntryToJournal() обрабатывает:
   - Обновляет signal: dev_log_signal.update([новая, ...старые])
   - Это срабатывает effect() → данные сохраняются в LocalStorage
   - localStorage.setItem('journal_dev_log', JSON.stringify(данные))
   ↓
5. UI автоматически перерисовывается:
   - @for видит новый Signals
   - Новая запись появляется в списке
   ↓
6. Пользователь перезагружает страницу:
   - JournalEntries.constructor() вызывает loadFromStorage()
   - loadFromStorage() восстанавливает данные из localStorage
   - Запись остается на месте ✅
```

### Сценарий 3: Запрос к Gemini

```
1. Пользователь пишет вопрос в input
   - question обновляется (two-way binding)
   ↓
2. Пользователь нажимает "Спросить Близнецов"
   - ask_question() вызывается
   ↓
3. ask_question() проверяет:
   - Что question не пуст
   - Что api_key не пуст
   - Что api_key длиннее 20 символов
   ↓
4. Если всё окей, вызывает ask(question)
   - Устанавливает loading = true (показывает спиннер)
   - Собирает все записи из journalEntries.getEntries()
   - Создает большой промпт с датой, инструкцией и записями
   ↓
5. Вызывает callGemini(prompt):
   - Создает GoogleGenerativeAI клиент с api_key
   - Вызывает model.generateContent(prompt)
   - Это REST запрос к https://generativelanguage.googleapis.com
   ↓
6. Google API обрабатывает запрос:
   - Gemini 3 Flash анализирует записи
   - Возвращает JSON ответ
   ↓
7. callGemini() обрабатывает ответ:
   - Если успешно → возвращает text()
   - Если ошибка API → возвращает "-2" (проблема с ключом)
   - Если другая ошибка → возвращает "-1"
   ↓
8. ask() обрабатывает результат:
   - Если "-2" → показывает ошибку о ключе
   - Если "-1" → показывает общую ошибку
   - Если текст → показывает answer в красивом блоке
   - Устанавливает loading = false (скрывает спиннер)
   ↓
9. UI обновляется:
   - Пользователь видит ответ Gemini
   - Ответ форматируется построчно (split("\n"))
```

### LocalStorage vs SessionStorage

| Особенность | LocalStorage | SessionStorage |
|------------|-------------|----------------|
| **Время жизни** | До ручной очистки | До закрытия вкладки |
| **Размер** | ~5-10 MB | ~5-10 MB |
| **Синхронизация** | Между вкладками | Только в одной вкладке |
| **Безопасность** | Средняя | Лучше (меньше живет) |
| **Использование** | Записи журнала | API ключ |

**Почему так?**
- 📝 **Записи** в LocalStorage → остаются между сеансами (пользователь их хочет)
- 🔑 **Ключ** в SessionStorage → удаляется при закрытии браузера (безопаснее)

---

## 🚀 Цели для доработки

### Приоритет 1: КРИТИЧНО

#### 1.1 Backend API Proxy
**Проблема:** API ключ видим в браузере (DevTools → Network)
**Решение:** Создать Node.js/Python backend, который будет:
- Хранить API ключ на сервере
- Принимать запросы с фронта
- Отправлять запросы к Gemini от сервера
- Возвращать результат на фронт

**Файлы:**
```
backend/
├── server.js            # Express сервер
├── routes/gemini.js     # Endpoints для Gemini
└── .env                 # API_KEY (на сервере!)
```

**Пример:**
```typescript
// Было (небезопасно):
const genAI = new GoogleGenerativeAI(this.api_key);  // ключ в браузере

// Станет (безопасно):
const response = await fetch('/api/gemini/generate', {
  method: 'POST',
  body: JSON.stringify({ prompt: this.question })
});
```

#### 1.2 Аутентификация пользователя
**Для чего:** Разные пользователи → разные данные

**Сервисы:** Firebase Auth или Supabase Auth
**Что добавить:**
-登録/Вход (Sign up / Sign in)
- Персональный профиль
- Синхронизация данных с облаком

#### 1.3 Хранилище в облаке
**Для чего:** Доступ к данным с разных устройств

**Сервисы:**
- Firebase Firestore
- Supabase PostgreSQL
- MongoDB Atlas

**Миграция:**
```typescript
// Было:
localStorage.setItem('journal_dev_log', ...)

// Станет:
firestore.collection('users').doc(userId).collection('journals').add(entry)
```

---

### Приоритет 2: ВАЖНО

#### 2.1 Поиск по записям
```typescript
// Новый метод в JournalEntries
searchEntries(query: string, journal: string): JournalEntry[] {
  const entries = this.getEntries(journal);
  return entries.filter(e => 
    e.entry.toLowerCase().includes(query.toLowerCase()) ||
    e.date.includes(query)
  );
}

// В UI:
<input [(ngModel)]="searchQuery" placeholder="Поиск..." />
<button (click)="performSearch()">🔍</button>
```

#### 2.2 Теги и категории
```typescript
interface JournalEntry {
  date: string;
  entry: string;
  tags?: string[];        // Новое поле
  category?: string;      // Новое поле
  priority?: 'низкий' | 'средний' | 'высокий';
}
```

#### 2.3 Экспорт данных
```typescript
// Экспорт в JSON
exportAsJSON() {
  const data = this.dev_log_signal();
  const json = JSON.stringify(data, null, 2);
  download(json, 'journal-export.json');
}

// Экспорт в PDF
exportAsPDF() {
  // Использовать библиотеку pdfkit или html2pdf
}

// Экспорт в Markdown
exportAsMarkdown() {
  // Преобразовать в .md файл
}
```

#### 2.4 Dark Mode
```typescript
// Добавить toggle в UI
isDarkMode = signal(false);

// В app.config.ts
providers: [
  provideScafolding(),
  { provide: DARK_MODE, useValue: isDarkMode }
]

// В styles
@media (prefers-color-scheme: dark) {
  body { background: #1a1a1a; }
}
```

#### 2.5 Хронология и статистика
```typescript
// Новый компонент: StatsComponent
interface Stats {
  totalEntries: number;
  entriesToday: number;
  entriesThisWeek: number;
  avgWordsPerEntry: number;
  mostActiveDay: string;
}

// Граф активности
showActivityChart() {
  // Chart.js или D3.js
}
```

---

### Приоритет 3: ФИЧИ

#### 3.1 PWA (Progressive Web App)
**Что дает:** Установка как приложение, offline режим

```bash
# Добавить в проект
ng add @angular/pwa
```

#### 3.2 Интеграции
- Slack Notifications
- Discord Bot
- Telegram Bot
- Email Summary (еженедельный отчет)

#### 3.3 AI Features
- Автоответы (AI дополняет запись)
- Sentiment Analysis (анализ тона записей)
- Recommendations (рекомендации на основе истории)
- Summarization (краткое резюме за неделю)

#### 3.4 Collaborative Features
- Шаринг записей
- Комментарии других пользователей
- Совместное редактирование

#### 3.5 Mobile App
- Использовать Ionic Framework
- Или React Native
- Or Flutter

---

## 📋 Процесс разработки и коммиты

### Версионирование

Используем [Semantic Versioning](https://semver.org/):
- **Major (1.0.0)** — огромные изменения
- **Minor (1.1.0)** — новые фичи
- **Patch (1.0.1)** — баг-фиксы

### Структура коммита

```
тип: краткое описание (до 50 символов)

Подробное описание (если нужно)
- Пункт 1
- Пункт 2

Closes #123 (если закрывает issue)
```

**Типы коммитов:**
- `feat:` — новая фича
- `fix:` — исправление бага
- `docs:` — изменение документации
- `style:` — форматирование кода (без логики)
- `refactor:` — переделка кода (то же поведение)
- `perf:` — улучшение производительности
- `test:` — добавление тестов
- `chore:` — обновление зависимостей

### История коммитов v1.0.0

```
commit 019481e
feat: v1.0.0 - Production-ready Journal with Gemini
- LocalStorage для персистентных записей
- SessionStorage для API ключа
- Улучшена обработка ошибок
- Deploy конфиги для Vercel, Netlify, Cloudflare
- Полная документация в README
- Удалены хардкодированные ключи

commit 87b9db8
feat: первая загрузка кода Journal Buddy 2.0
```

### Git Workflow

#### Создание новой фичи

```bash
# 1. Создай новую ветку
git checkout -b feature/search-entries

# 2. Разрабатывай
# ... пиши код ...

# 3. Коммитируй регулярно
git add src/app/search.service.ts
git commit -m "feat: добавлен сервис поиска записей"

git add src/app/
git commit -m "feat: UI компонент поиска"

# 4. Раз в день запускай тесты
npm test

# 5. Когда готово, делай pull request
git push origin feature/search-entries
# Затем на GitHub: Create Pull Request

# 6. После review, merge в main
git checkout main
git pull origin main
git merge feature/search-entries
git push origin main

# 7. Удали ветку
git branch -d feature/search-entries
```

#### Обновление версии

```bash
# В package.json
{
  "version": "1.1.0"  // было 1.0.0
}

# Коммит
git add package.json
git commit -m "chore: bump version to 1.1.0"

# Tag
git tag -a v1.1.0 -m "Release v1.1.0"
git push origin main --tags
```

---

## 📚 Как добавить новую фичу (пошаговый пример)

### Пример: Добавляем поиск по записям

#### Шаг 1: Создать ветку
```bash
git checkout -b feature/search-functionality
```

#### Шаг 2: Добавить метод в JournalEntries
```typescript
// В src/app/journal-entries.ts

export class JournalEntries {
  // ... существующий код ...

  /**
   * Поиск записей по запросу
   */
  searchEntries(query: string, journal: string): JournalEntry[] {
    if (!query || query.trim().length === 0) {
      return this.getEntries(journal);
    }

    const entries = this.getEntries(journal);
    const lowercaseQuery = query.toLowerCase();

    return entries.filter(entry =>
      entry.entry.toLowerCase().includes(lowercaseQuery) ||
      entry.date.includes(query)
    );
  }
}
```

#### Шаг 3: Добавить UI в JournalComponent
```html
<!-- В template JournalComponent -->
<div class="search_box">
  <input 
    type="text" 
    [(ngModel)]="searchQuery"
    placeholder="🔍 Поиск по записям..."
  />
  <button (click)="performSearch()">Найти</button>
</div>

<!-- Вместо всех записей показываем результаты поиска -->
<h1 class="journal_entries_header">
  {{ searchResults.length > 0 ? 'Результаты поиска' : 'Все записи' }}
</h1>
<div class="entries_container">
  @for(entry of searchResults; track entry) {
    <app-entry [entry]="entry" />
  }
</div>
```

#### Шаг 4: Добавить логику в TypeScript
```typescript
export class JournalComponent {
  // ... существующее ...
  
  searchQuery = "";
  searchResults: JournalEntry[] = [];

  performSearch() {
    this.searchResults = this.journalEntries.searchEntries(
      this.searchQuery,
      this.selected_journal
    );
  }

  // Также необходимо обновить при выборе журнала
  ngOnInit() {
    this.searchResults = this.journalEntries.getEntries(this.selected_journal);
  }
}
```

#### Шаг 5: Добавить стили
```css
.search_box {
  padding: 20px;
  margin: 20px;
  background: #f8fbff;
  border: 1px solid #1a73e8;
  border-radius: 12px;
  display: flex;
  gap: 10px;
}

.search_box input {
  flex: 1;
  padding: 10px;
  border: 1px solid #dadce0;
  border-radius: 6px;
  font-size: 14px;
}

.search_box button {
  padding: 10px 20px;
  background: #1a73e8;
  color: white;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-weight: bold;
}

.search_box button:hover {
  background: #1666cc;
}
```

#### Шаг 6: Тестируем локально
```bash
npm start
# Открываем http://localhost:4200
# Тествируем поиск
```

#### Шаг 7: Коммитим
```bash
git add src/app/journal-entries.ts
git add src/app/journal.component.ts
git commit -m "feat: добавлена функция поиска по записям

- Метод searchEntries() в JournalEntries сервисе
- UI компонент с input для поиска
- Фильтрация по содержимому и дате
- Стили для поиска"
```

#### Шаг 8: Push и Pull Request
```bash
git push origin feature/search-functionality
# Pada на GitHub, создаешь PR, описываешь изменения
```

#### Шаг 9: Merge и версионирование
```bash
git checkout main
git pull origin main
git merge feature/search-functionality
git push origin main

# Обновляем версию
# В package.json: "version": "1.1.0"
git add package.json
git commit -m "chore: bump version to 1.1.0"
git tag -a v1.1.0 -m "Release v1.1.0: Add search functionality"
git push origin main --tags
```

---

## 🔍 Как читать и понимать этот код

### Если ты новичок в Angular

**Ключевые концепции:**
1. **Components** — это кусочки UI (как строительные блоки)
2. **Services** — это логика (как помощники для компонентов)
3. **Templates** — это HTML (@if, @for, {{переменная}})
4. **Signals** — это реактивные переменные (автоматически обновляют UI)
5. **Two-way binding** — `[(ngModel)]` синхронизирует input и переменную

#### Пример чтения кода

```typescript
// ЭТО:
export class JournalComponent {
  newEntryText = "";           // Переменная для текста записи
  
  addNewEntry() {              // Функция (вызывается при нажатии кнопки)
    if (!this.newEntryText.trim()) return;  // Если пусто → выход
    
    const today = new Date().toISOString().split('T')[0];  // Дата
    const newEntry = {          // Создаем объект
      date: today,
      entry: `--- \n status: Logged \n --- \n ${this.newEntryText}`
    };
    
    this.journalEntries.addEntryToJournal(this.selected_journal, newEntry);
    // ↑ Вызываем сервис чтобы сохранить запись
    
    this.newEntryText = "";     // Очищаем поле
  }
}
```

**ПЕРЕВОДИТСЯ КАК:**
```
1. Создай переменную newEntryText
2. Когда пользователь нажимает "Сохранить":
   a) Проверь что текст не пуст
   b) Получи сегодняшнюю дату
   c) Создай объект записи
   d) Отправь его в сервис (он сохранит)
   e) Очисти поле ввода
```

---

## 📞 Документация ссылки

- [Angular Documentation](https://angular.dev)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Google Generative AI SDK](https://ai.google.dev/tutorials/node_quickstart)
- [Web Storage API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Storage_API)
- [Git Tutorial](https://git-scm.com/docs)

---

## 🎯 Заключение

Этот документ — ваша **дорожная карта** разработки. Каждая новая фича должна:
1. Быть описана в этом файле
2. Иметь пошаговые инструкции
3. Включать пример кода
4. Быть задокументирована в коже

**Помните:** Код пишется один раз, а читается сто раз. Делайте его понятным! 🙏

---

**Последнее обновление:** 8 февраля 2026  
**Версия документа:** 1.0.0  
**Автор:** Исходящий из глубин ИИ Апостол Кодинга 🤖✨
